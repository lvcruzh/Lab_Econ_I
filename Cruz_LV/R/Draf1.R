#Comentarios en R
getwd() # Da el directorio de trabajo
setwd("C://Users/Luis Valentin Cruz/Documents/GitHub/Lab_Econ_I/Cruz_LV") #Fijar el directorio
#Funciones aritméticas
1+1
#abs(x) sqrt(x) round(x, digits=n) log(x) exp(x)
abs(-5)
sqrt(36)
round(2.5678,2)
log(1)
exp(3)
#Variables en R
D="matrix c"
#Class() para saber el tipo de dato o variable, llamar variables
D
#creando un vector
Vectora = c(5,4,3,2,1)
vectora #llamar al vector nombrado anteriormente
#Matrices
F = matrix(c())
#Matriz transpuesta
G = t(F)
#Matriz identica
I=diag(5)
# Elementos de una matriz
#Inversa de una matriz
INV=solve(F)
#Concatenar
#P=cbind(a,o) en columnas y rbind para concatenar en rows

